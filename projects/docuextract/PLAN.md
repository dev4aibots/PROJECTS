# Plan

Define invoice and verification contracts; implement Decimal engine; add validated upload/repository/extractor boundaries; expose document API; generate known-ground-truth samples; build verification console; run failure tests/eval/build; audit and hand off cloud-only steps.
